# Config (contains passwords & API keys - NEVER commit!)
config.php
deploy-secret.txt

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temp / Cache (keep deny-all htaccess)
tmp/*
!tmp/.htaccess
temp/
cache/
*.cache

# Backup
*.bak
*.backup

# Child panel runtime configs (keep deny-all htaccess)
storage/*
!storage/.htaccess

# User uploads (keep uploads/.htaccess via negation)
uploads/*
!uploads/.htaccess

# Security/local files (Codex senior audit)
.env
.env.*
!.env.example
!.env.sample
*.env.bak
*.env.backup
*secret*.json
*service-account*.json
firebase-service-account.json
google-services.json
*.pem
*.key
*.p8
*.p12
*.jks
*.keystore
secrets/
.secrets/

# Android / iOS build artifacts
android-app/.gradle/
android-app/.kotlin/
android-app/app/build/
android-app/branding-tmp/
android-app/local.properties
ios-app/build/
ios-app/DerivedData/
