# راهنمای توسعه — SMM Turk Panel

این سند اولویت‌های توسعه و بهبود پروژه را بر اساس گزارش نقاط ضعف و وضعیت فعلی کد لیست می‌کند.

---

## اولویت بالا (امنیت و پایداری)

| # | مورد | اقدام | فایل/محل | وضعیت |
|---|------|--------|-----------|--------|
| 1 | **SECRET_KEY ثابت** | در production از env یا مقدار ثابت ۳۲ کاراکتر hex استفاده شود؛ نه `random_bytes` در هر بار اجرا | `config.example.php`، مستندات نصب | ✅ مستند در README و config |
| 2 | **Session cookie امن** | قبل از `session_start()` تنظیم: `HttpOnly`, `Secure` (در HTTPS), `SameSite=Lax` | `app/Auth.php` یا `app/init.php` | ✅ انجام شد (Auth.php) |
| 3 | **عدم افشای خطای DB** | در production جزئیات PDO به کاربر نشان داده نشود؛ فقط لاگ + پیام کلی | `app/Database.php` | ✅ انجام شد |
| 4 | **اولین ادمین بعد از نصب** | در README/CHECKLIST مرحله «ساخت اولین کاربر ادمین و حذف/تغییر پسورد پیش‌فرض» به وضوح ذکر شود | `docs/`, `README.md` | ✅ انجام شد (README + چک‌لیست) |

---

## اولویت متوسط (امنیت و یکپارچگی)

| # | مورد | اقدام | فایل/محل | وضعیت |
|---|------|--------|-----------|--------|
| 5 | **API Key فقط در body یا header** | قبول نکردن key از query string؛ فقط POST body یا هدر `X-API-Key`؛ در مستندات API قید شود | `api/v2.php`، `api-page.php` یا docs | ✅ انجام شد |
| 6 | **هماهنگی متن لندینگ با روش پرداخت** | فعلاً فقط کریپتو؛ متن‌های کارت، PayPal، Payeer و غیره حذف یا با «فعلاً فقط کریپتو» هماهنگ شود | `home.php`, `lang/*.php`, FAQ | ✅ انجام شد (lang en/tr/de/fr) |
| 7 | **Content-Security-Policy (CSP)** | اضافه کردن هدر CSP (حداقل به صورت report-only) برای کاهش XSS | `app/init.php` یا header مشترک | ✅ انجام شد (report-only) |
| 8 | **مقدار نمونه کیف پول** | در `config.example.php` آدرس واقعی با placeholder مثل `YOUR_ETH_OR_USDT_ADDRESS` عوض شود | `config.example.php` | ✅ انجام شد |
| 9 | **پیش‌فرض SMM_DEBUG در production** | پیش‌فرض `SMM_DEBUG = false`؛ فقط با env یا constant صریح روشن شود | `config.php` / `config.example.php` | ✅ انجام شد + SMM_PRODUCTION |

---

## اولویت متوسط (عملکرد و UX)

| # | مورد | اقدام | فایل/محل |
|---|------|--------|-----------|
| 10 | **Pagination یا lazy load سرویس‌ها** | در صفحه New Order با دسته «All» (۱۵۰۰ سرویس) یا در `services.php`؛ اسکرول بی‌نهایت یا صفحه‌بندی | `index.php`, `services.php` |
| 11 | **ایندکس جستجوی تیکت** | با رشد جدول `tickets` ایندکس روی `subject` / `order_id` یا full-text در نظر گرفته شود | migration جدید، `tickets.php` |
| 12 | **نسخه‌گذاری API** | برای تغییرات ناسازگار بعدی مسیر `/api/v3` یا پارامتر version تا کلاینت‌های قدیمی خراب نشوند | مستندات، `api/v2.php` |

---

## اولویت پایین (کیفیت کد و نگهداری)

| # | مورد | اقدام | فایل/محل |
|---|------|--------|-----------|
| 13 | **جداسازی CSS/JS** | استایل‌های مشترک در `assets/css` و اسکریپت‌های مشترک در `assets/js`؛ refactor تدریجی | `layouts/header.php`, `home.php`, فایل‌های صفحه |
| 14 | **خروجی ترجمه و XSS** | همه خروجی‌های متنی به HTML با `h()`؛ در صورت نیاز به HTML مجاز از allowlist تگ محدود | `home.php`, فایل‌های استفاده‌کننده از `__()` |
| 15 | **Rate limit لاگین** | در کنار IP، اختیاری محدودیت بر اساس ایمیل/شناسه تا در محیط‌های اشتراکی یک کاربر بقیه را قفل نکند | `app/RateLimit.php`, `login.php` |

---

## پیشنهادهای توسعهٔ جدید (فراتر از گزارش ضعف‌ها)

| # | مورد | توضیح |
|---|------|--------|
| 16 | **تست خودکار** | واحد تست (PHPUnit/Pest) برای Auth، OrderManager، API و منطق بحرانی؛ حداقل یک تست اتصال DB و یک تست ثبت‌نام/لاگین |
| 17 | **لاگ خطاها در فایل** | در production علاوه بر خاموش کردن `display_errors`، خطاها در فایل لاگ (مثلاً با Logger موجود) نوشته شوند | `app/init.php` | ✅ انجام شد (php_errors.log) |
| 18 | **بکاپ دیتابیس** | اسکریپت یا cron برای بکاپ دوره‌ای دیتابیس و ذخیره امن |
| 19 | **مانیتورینگ ساده** | یک endpoint مثل `/health` یا `/ping` برای uptime check؛ اختیاری بررسی اتصال به provider API | `health.php` | ✅ انجام شد |
| 20 | **مستندات نصب/چک‌لیست deploy** | سند CHECKLIST-DEPLOY با مراحل: config، SECRET_KEY، اولین ادمین، غیرفعال کردن install-db، SSL، backup |
| 21 | **اعتبارسنجی ورودی API** | در `api/v2.php` اعتبار سنجی سخت‌گیرانه‌تر برای action، service_id، link و quantity و برگرداندن خطای واضح |
| 22 | **تاریخ انقضای session** | اگر SESSION_LIFETIME استفاده نمی‌شود در session_start، اعمال آن (مثلاً با بررسی زمان آخرین فعالیت) |

---

## خلاصهٔ دستور کار پیشنهادی

1. **فاز ۱ (امنیت فوری):** موارد ۱، ۲، ۳، ۸، ۹  
2. **فاز ۲ (مستندات و شفافیت):** موارد ۴، ۶، ۲۰  
3. **فاز ۳ (API و هدرها):** موارد ۵، ۷، ۱۲، ۲۱  
4. **فاز ۴ (عملکرد):** موارد ۱۰، ۱۱  
5. **فاز ۵ (کیفیت و نگهداری):** موارد ۱۳، ۱۴، ۱۵، ۱۶، ۱۷، ۱۸، ۱۹، ۲۲  

برای جزئیات هر مورد به `docs/PROJECT-WEAKNESSES-REPORT.md` مراجعه کنید.
